import { colors } from '@mui/material'

// Define custom text colors based on the Material-UI color palette
const customColors = {
  textPrimary: colors.blue[800], // Custom primary text color using a shade of blue
  textSecondary: colors.grey[700] // Custom secondary text color using a shade of grey
}

export default customColors
